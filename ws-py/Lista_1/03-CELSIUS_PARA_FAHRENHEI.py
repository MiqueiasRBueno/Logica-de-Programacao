# Converte uma temperatura em Celsius para Fahrenheit

temp_celsius = float(input("Digite a temperatura desejada: "))
print(f"Temperatura em Celsius : {temp_celsius:.2f}"
      f"\nTemperatura em Fahrenheit: {(temp_celsius * 1.8) + 32:.2f}")